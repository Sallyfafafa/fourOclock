<script>    import global_ from '../components/Global.vue'    import tool from '../components/lib/util.js'    const modal = weex.requireModule('modal');    const stream = weex.requireModule('stream');    const storage = weex.requireModule('storage');    export default {        name: "jieshou",        created() {            this.payInfo();        },        methods: {            payInfo() {                let url = global_.dataUrl + "/api/getJson/getPayInfo";                let data;                var info = decodeURI(weex.config.bundleUrl);                let trade_no=tool.getUrlParam("trade_no");                let total_amount=tool.getUrlParam("total_amount");                let trade_status=tool.getUrlParam("trade_status");                data={                    info:info,                    trade_no: trade_no,                    total_amount:total_amount,                    trade_status:trade_status,                }                tool.sendRequest(url, data, ret => {                });            },        },    }</script>